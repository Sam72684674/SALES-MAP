from fastapi import APIRouter, HTTPException\nfrom typing import List, Optional\n\nrouter = APIRouter()\n\nclass ZoneIn:\n    name: str\n    type: str\n    parent_id: Optional[int] = None\n\nclass ZoneOut:\n    id: int\n    name: str\n    type: str\n    parent_id: Optional[int] = None\n    children: List['ZoneOut'] = []\n\nzones = []  # This would normally be a database or any persistent storage\n\n@router.get('/', response_model=List[ZoneOut])\nasync def list_zones(type: Optional[str] = None):\n    if type:\n        return [zone for zone in zones if zone['type'] == type]\n    return zones\n\n@router.post('/', response_model=ZoneOut)\nasync def create_zone(zone: ZoneIn):\n    if zone.parent_id and zone.parent_id >= len(zones):\n        raise HTTPException(status_code=400, detail="Parent zone does not exist")\n    new_zone = {\n        'id': len(zones),\n        'name': zone.name,\n        'type': zone.type,\n        'parent_id': zone.parent_id\n    }\n    zones.append(new_zone)\n    return new_zone\n\n@router.get('/{id}', response_model=ZoneOut)\nasync def get_zone(id: int):\n    if id >= len(zones):\n        raise HTTPException(status_code=404, detail="Zone not found")\n    return zones[id]\n\n@router.put('/{id}', response_model=ZoneOut)\nasync def update_zone(id: int, zone: ZoneIn):\n    if id >= len(zones):\n        raise HTTPException(status_code=404, detail="Zone not found")\n    updated_zone = {**zones[id], **zone.dict()}\n    zones[id] = updated_zone\n    return updated_zone\n\n@router.delete('/{id}')\nasync def delete_zone(id: int):\n    if id >= len(zones):\n        raise HTTPException(status_code=404, detail="Zone not found")\n    zones.pop(id)\n    return "Zone deleted"\n\n@router.get('/{id}/children', response_model=List[ZoneOut])\nasync def get_children(id: int):\n    if id >= len(zones):\n        raise HTTPException(status_code=404, detail="Zone not found")\n    return [zone for zone in zones if zone['parent_id'] == id]\n\n@router.post('/tree', response_model=List[ZoneOut])\nasync def create_full_hierarchy(tree: List[ZoneIn]):\n    for zone in tree:\n        await create_zone(zone)\n    return zones